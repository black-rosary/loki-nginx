global:
  resolve_timeout: 5m

route:
  receiver: 'webhook_localhost'
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 20m


receivers:
  - name: 'webhook_localhost'
    webhook_configs:
     - send_resolved: true
       url: "http://localhost/alert"

